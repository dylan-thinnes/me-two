<%= render partial: "shared/section_colour", locals: { path: request.path, colour: colours[action_name.to_sym]} %>
<% set_title("Dylan's Blog") %>
<%= to_markdown "## blog" %>
<% Article.where(published: true).each do |a| %>
  <%= to_markdown "
  **[#{a.title}](/article/#{a.id})**
  #{a.blurb}

  " %>
<% end %>
